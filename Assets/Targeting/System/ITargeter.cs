namespace Targeting
{
    /// <summary>
    /// Marker / identity interface for anything that owns a TargetingContextModel.
    /// Implemented by player and AI targeters.
    /// </summary>
    public interface ITargeter
    {
        // You can add members later if you need them (e.g. Transform Center, Faction, etc.)
    }
}
